CREATE OR REPLACE PROCEDURE sum_up_to(n IN NUMBER) AS
    s NUMBER := 0;
    i NUMBER := 1;
BEGIN
    WHILE i <= n LOOP
        s := s + i;
        i := i + 1;
    END LOOP;
    DBMS_OUTPUT.PUT_LINE('Sum = ' || s);
END;
/

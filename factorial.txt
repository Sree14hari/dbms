CREATE OR REPLACE PROCEDURE factorial(n IN NUMBER) AS
    f NUMBER := 1;
    i NUMBER := 1;
BEGIN
    WHILE i <= n LOOP
        f := f * i;
        i := i + 1;
    END LOOP;
    DBMS_OUTPUT.PUT_LINE('Factorial = ' || f);
END;
/

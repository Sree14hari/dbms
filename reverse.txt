DELIMITER $$

CREATE PROCEDURE reverse_num(IN n INT)
BEGIN
    DECLARE rev INT DEFAULT 0;
    DECLARE rem INT;

    WHILE n > 0 DO
        SET rem = MOD(n, 10);
        SET rev = rev * 10 + rem;
        SET n = FLOOR(n / 10);
    END WHILE;

    SELECT rev AS Reversed;
END$$

DELIMITER ;
